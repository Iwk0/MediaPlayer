<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.mediaplayer.activity.TrackInfoActivity">

    <TextView
        android:id="@+id/trackNameView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/track"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/trackDurationView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/trackNameView"
        android:layout_marginTop="40dp"
        android:text="@string/duration"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/fileLengthView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/trackDurationView"
        android:layout_marginTop="40dp"
        android:text="@string/file_length"
        android:textSize="20sp" />

</RelativeLayout>